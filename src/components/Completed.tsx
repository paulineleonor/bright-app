import React from "react";
import { FunctionComponent } from "react";

export type CompletedProps = {
    date: string;
}

export const Completed: FunctionComponent<CompletedProps> = ({ date }) => {
    return (
        <div>
           // build completed block, display date and fetch results on button click
        </div>
    )
}